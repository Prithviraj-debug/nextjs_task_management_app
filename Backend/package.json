{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index",
    "json-server": "json-server --watch data/tasks.json --port 3001"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3",
    "node-fetch": "^3.3.1",
    "nodemon": "^3.0.1",
    "scripts": "^0.1.0"
  }
}
